settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: go_rest_sample
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
